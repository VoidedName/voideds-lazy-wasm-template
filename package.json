{
  "name": "wasm",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "cd wasm && wasm-pack build --release --target web --out-dir ../pkg --out-name assembly && cd .. && tsc && vite build"
  },
  "devDependencies": {
    "typescript": "^5.0.4",
    "vite": "^4.3.8"
  },
  "dependencies": {
    "@types/node": "^20.2.1"
  }
}
